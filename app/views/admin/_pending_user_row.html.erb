<tr>
   <td><%= user.username %></td>
  	<td><%= user.email %></td>
  	<td>
  		<% if user.player == nil %>
  			Ingen spiller (<%= link_to 'Vælg...', '#' %>)
  		<% else %>
  			<%= link_to(user.player.name, user.player) %>
 		<% end %> 
  	</td>
   <td>
      <%= link_to('Godkend', approve_user_path(user), { :class => 'btn btn-mini btn-success de-activated'}) %>
   </td>
   <td>
      <%= link_to('Afvis', reject_user_path(user), { :class => 'btn btn-mini btn-danger'}) %>
   </td>
</tr>