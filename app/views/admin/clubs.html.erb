<% provide(:title, 'Administrator') %>
<h1>Administrer klubber</h1>

<%= link_to('Ny klub', new_club_path, class: "btn btn-large btn-primary") %>
<br></br>

<table class="table table-striped table-condensed table-bordered">
	<tr>
		<th>Navn</th>
		<th>Adresse</th>
		<th>Antal borde</th>
		<th></th>
	</tr>
	<% @clubs.each do |club| %>
   		<%= render 'club_row', :club => club %>	
  	<% end %>
</table>