<%= form_for(current_user.player.enter.build(tournament_id: @tournament.id)) do |f| %>
	<div><%= f.hidden_field :tournament_id %></div>
	<div><%= f.hidden_field :player_id %></div>
	<%= f.submit "Tilmeld", class: "btn btn-large btn-primary" %>
<% end %>
