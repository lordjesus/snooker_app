<% provide(:title, 'Rangliste') %>
<h1>Rangliste</h1>

<ul class="players">
  <% @players.each do |player| %>
    <li>      
      <%= link_to player.name, player %>, <%= link_to(player.club.name, player.club) %>, <%= player.ranking_points %>
    </li>    
  <% end %>
</ul>
